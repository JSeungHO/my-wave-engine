# MyAutomationEngine — 진행 상태

> 최종 갱신: 2026-05-25 (Phase 2c Task 2c-1~3 완료)

## Claude.ai 세션 완료 (2026-05-25)

| 배치 | 내용 | 파일 수 |
|------|------|---------|
| Batch A | core/ 전체 복원 | 7 |
| Batch B | adapters/three/ 복원 | 3 |
| Batch C | demo/ + 루트 복원 | 5 |
| Task 2-2 | GerstnerWaterPrimitive.js 재완성 | 1 |
| — | cesium/index.js + INTEGRATION.md 복원 | 2 |
| Task 2-4 | Cesium 단독 데모 (cesium.html, cesium-main.js) | 2 |

동 세션 추가: Task 2-1 `TangentPlane.js`, Task 2-3 `FloatingEntity.js`

---

## 프로젝트 목표

**Gerstner Wave 기반 WebGL 오션**을 Three.js로 구현하고, `core` 모듈을 분리해 **Cesium.js** 에 이식 가능하게 만든다.

| 구분 | 설명 |
|------|------|
| 엔진 | `MyAutomationEngine` — .NET 10, `dotnet run` → 폴더/파일 생성 |
| 산출물 | `MyWaveCompany_Generated/` — WebGL Gerstner Ocean |
| 1차 런타임 | Three.js + Vite (`npm run dev` → `index.html`) |
| 2차 이식 | Cesium.js (`npm run dev` → `cesium.html`) |
| 참고 | [GPU Gems Ch.1](https://developer.nvidia.com/gpugems/gpugems/part-i-natural-effects/chapter-1-effective-water-simulation-physical-models) |

---

## ⚠️ dotnet run 주의

`dotnet run` 실행 시 기획서 **생성 대상** 파일 중 **신규**만 `// TODO` 로 생성됩니다.  
**이미 존재하는 파일은 `[공장] 보호됨` 으로 건너뜁니다.**

`TangentPlane.js`, `FloatingEntity.js`, `cesium-main.js`, `cesium.html`, docs/ 등은 생성 목록에 없으므로 항상 안전합니다.

---

## 현재 진행 상태

### ✅ Phase 1 완료 — Core + Three.js WebGL 데모

| 항목 | 위치 |
|------|------|
| Gerstner CPU 솔버 | `core/math/GerstnerWave.js` |
| Gerstner GPU 셰이더 | `core/shaders/*.glsl` |
| Three.js 어댑터 | `adapters/three/OceanMaterial.js`, `OceanMesh.js` |
| WebGL 데모 | `demo/main.js` — 파도 + 보트·부표 부력 |
| Vite 빌드 | `npm run dev` → `index.html` |
| waves.json | `configs/waves.json` |

### ✅ Phase 1.5 완료 — 문서화

`docs/ARCHITECTURE.md`, `docs/API.md`, `docs/CONFIG.md`, `docs/SHADER.md`,  
`core/README.md`, `adapters/three/README.md`, `adapters/cesium/INTEGRATION.md`

### ✅ Phase 2 완료 — Cesium.js 이식

| Task | 파일 | 내용 |
|------|------|------|
| 2-1 | `adapters/cesium/TangentPlane.js`          | WGS84/ECEF ↔ ENU 좌표 변환 |
| 2-2 | `adapters/cesium/GerstnerWaterPrimitive.js` | Cesium Primitive + Material 파도 렌더링 |
| 2-3 | `adapters/cesium/FloatingEntity.js`         | Entity 위치·기울기 수면 추종 |
| 2-4 | `cesium.html` + `adapters/cesium/cesium-main.js` | Three.js 없는 독립 Cesium 데모 |

#### Phase 2 핵심 구조

```
GerstnerWaterPrimitive (Cesium Primitive + Material)
  └── TangentPlane (WGS84 ↔ ENU 변환)
  └── GerstnerWave CPU 솔버 (core/ 공유)

FloatingEntity (Entity position/orientation CallbackProperty)
  └── TangentPlane.getWaterAltitude()
  └── TangentPlane.getWaveNormalEnu() → ECEF quaternion slerp
```

#### Phase 2b 완료 (GPU 버텍스 변위)

| Task | 파일 | 내용 |
|------|------|------|
| 2b-1 | 설계 | ENU 격자 + GLSL ES 1.00 동적 VS 설계 확정 |
| 2b-2 | `adapters/cesium/GerstnerWaterPrimitiveGPU.js` | ENU N×N Geometry + custom Appearance + _commands 인터셉트 |
| 2b-3 | `cesium-main.js` + `INTEGRATION.md` 업데이트 | GPU 모드 드롭인 연동 + 문서 완료 |

```
GerstnerWaterPrimitiveGPU (Phase 2b — 현재 cesium-main.js 사용)
  ├── buildEnuGeometry(widthM, heightM, res)  → a_enuPos + a_st
  ├── buildVertexShader(waves)                → GLSL ES 1.00 인라인 파라미터
  ├── primitive.modelMatrix = enuToEcef       → czm_model = ENU→ECEF
  └── _commands uniformMap 인터셉트           → u_time / u_deepColor / u_shallowColor
```

### ✅ Phase 2c 착수 — Wake 상호작용 MVP (Task 2c-1~3 완료)

| Task | 파일 | 내용 |
|------|------|------|
| 2c-1 | `configs/interaction.json` | Wake/Collision/Performance 설정 |
| 2c-1 | `core/types/InteractionTypes.js` | WakeSource·CollisionBody 타입 + packWakeSources (Cesium 금지) |
| 2c-1 | `core/math/WakeField.js` | CPU Wake 링 파도 솔버 (Cesium 금지) |
| 2c-1 | `core/index.js` | WakeField·InteractionTypes export 추가 |
| 2c-1 | `Templates/` | gen_interaction.json, gen_WakeField.js, gen_wake.glsl 스텁 |
| 2c-2 | `adapters/cesium/WakeRegistry.js` | FloatingEntity 속도 → WakeField 소스 관리 + ocean GPU 연결 |
| 2c-2 | `adapters/cesium/FloatingEntity.js` | 속도 추적 + WakeRegistry 자동 등록, destroy 시 비활성화 |
| 2c-3 | `core/shaders/wake.glsl` | GPU Wake GLSL 함수 (wakeDisplacement) |
| 2c-3 | `adapters/cesium/GerstnerWaterPrimitiveGPU.js` | Wake uniform 배열 + updateWakeSources() |
| 2c-3 | `adapters/cesium/cesium-main.js` | WakeRegistry + 선박 Wake 연동 |

#### Phase 2c 미착수

| Task | 내용 |
|------|------|
| 2c-4 | HeightFieldCache.js — Entity CPU 최적화 (TD-1) |
| 2c-5 | FloatingEntity.getSubmergeRatio() — Collision draft/submerge |
| 2c-6 | TileManager.js — 다중 타일 PoC (TD-2) |

### ❌ Phase 3 미착수 — 자동화 엔진 강화

- `Program.cs` 템플릿 기반 실제 코드 생성
- `waves.json` → 생성 코드 파라미터 주입
- 생성 후 필수 파일·API 존재 검증 스크립트

---

## 실행 방법

```bash
cd MyWaveCompany_Generated
npm install      # 첫 실행 시
npm run dev
```

- **Three.js 데모**: http://localhost:5173/  (`index.html`)
- **Cesium 데모**:   http://localhost:5173/cesium.html

> Cesium 데모는 `cesium-main.js` 에서 `Cesium.Ion.defaultAccessToken` 을 자신의 토큰으로 교체해야 합니다.

---

## 성공 기준 (Definition of Done)

### Phase 1 ✅
1. `npm run dev` 로 Gerstner 파도 렌더링
2. `getWaterHeight()` 로 부표·보트 수면 추종
3. `core/` 가 Three.js import 없이 독립 동작

### Phase 2 ✅
4. `core/` 복사만으로 Cesium 프로젝트에서 동일 파도 수식 사용
5. `TangentPlane.getWaterAltitude()` 로 WGS84 수면 고도 계산
6. Cesium Entity 고도·기울기가 Gerstner 파도를 실시간 추종
7. Three.js 없이 `cesium.html` 단독으로 파도 + Entity 데모 실행

### Phase 3 (목표)
8. `dotnet run` 한 번으로 의미 있는 코드 생성
9. `waves.json` 변경이 생성 결과에 반영

---

## 관련 문서

| 문서 | 경로 |
|------|------|
| 기획서 | [기획서.md](./기획서.md) |
| WebGL 프로젝트 | [MyWaveCompany_Generated/README.md](./MyWaveCompany_Generated/README.md) |
| 아키텍처 | [docs/ARCHITECTURE.md](./MyWaveCompany_Generated/docs/ARCHITECTURE.md) |
| Core API | [docs/API.md](./MyWaveCompany_Generated/docs/API.md) |
| Cesium 이식 | [INTEGRATION.md](./MyWaveCompany_Generated/adapters/cesium/INTEGRATION.md) |
